import type { Metadata } from "next";import { categories,fields } from "@/lib/products";
export const metadata:Metadata={title:"Green Coffee E-Catalogue",description:"Complete Arabica, Robusta and specialty green coffee specifications from Rihla Global.",alternates:{canonical:"/e-catalogue"}};
export default function Catalogue(){return <main id="main"><section className="page-hero"><img src="/images/beans.jpg" alt="Green coffee beans"/><div className="page-hero-content"><p className="kicker">Editorial specification register</p><h1>E-catalogue</h1><p>All current grades in one buyer-ready reference.</p></div></section><section className="section">{categories.map(c=><section className="catalogue-category" key={c.slug}><p className="kicker">{c.eyebrow}</p><h2>{c.title}</h2><table className="spec-table"><thead><tr>{fields.map(([,l])=><th key={l}>{l}</th>)}</tr></thead><tbody>{c.grades.map(g=><tr key={g.name}>{fields.map(([k])=><td key={k}>{g[k]}</td>)}</tr>)}</tbody></table></section>)}<p className="trust-note">Grading and garbling per Coffee Board of India standards. Every lot ships with a Certificate of Analysis covering moisture, screen retention, and defect count. Contract specifications govern each confirmation.</p><p style={{marginTop:"2rem"}}><a className="text-link" href="https://www.rihlaglobal.com/e-catalogue.html" target="_blank" rel="noopener noreferrer">Open legacy catalogue ↗</a></p></section></main>}